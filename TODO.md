# TODO: План развития проекта

## 1. Торговая интеграция (Real Trading)
- [ ] Добавить библиотеку `python-binance` (или ccxt для мультибиржевости).
- [ ] Реализовать систему управления API ключами (хранение в `.env` файле, использование `python-dotenv`).
- [ ] Заменить текстовые принты сигналов (BUY/SELL) на реальные или тестовые (Testnet) API-вызовы к бирже для открытия рыночных или лимитных ордеров.
- [ ] Реализовать проверку баланса перед открытием сделки.

## 2. Риск-менеджмент (Risk Management)
- [ ] **Stop-Loss (SL):** Добавить автоматическое закрытие позиции (по рынку), если убыток по ней превышает заданный порог (например, 1%).
- [ ] **Take-Profit (TP):** Добавить автоматическую фиксацию прибыли при достижении цели, даже если нейросеть еще не дала обратный сигнал.
- [ ] **Trailing Stop:** Подтягивание стоп-лосса вслед за растущей ценой для максимизации прибыли.

## 3. Улучшение работы с данными
- [ ] Добавить обработку объемов торгов (Volume). Сейчас сеть смотрит только на цену (1D тензор). Если передавать цену + объем (2D тензор), нейросеть сможет лучше понимать силу тренда.
- [ ] Добавить анализ стакана (Order Book) для самых быстрых (HFT) скриптов.
- [ ] Реализовать автоматическую динамическую подстройку скейлера (`MinMaxScaler`). Сейчас границы жестко заданы (50k - 100k). Если Биткоин выйдет за эти пределы, скрипт нужно будет перезапускать с новыми настройками.

## 4. Оптимизация нейросети (LTC)
- [ ] Провести эксперименты с количеством нейронов (`NEURONS` 16 vs 32 vs 64 vs 128) и длиной памяти (`SEQ_LENGTH`) для поиска оптимального соотношения скорость/качество.
- [ ] Добавить механизм динамического изменения `LEARNING_RATE`. Во время высокой волатильности на рынке скорость обучения можно повышать, а во время флэта (штиля) - понижать, чтобы избежать ложных срабатываний.
- [ ] Сохранять не только веса модели (`.pth`), но и параметры скейлера, чтобы при перезапуске нормализация данных не сбивалась.

## 5. Мониторинг и UI
- [x] Перенести вывод графиков из локальных окон `matplotlib` (которые блокируют работу в консоли без X-сервера) в веб-интерфейс (например, с использованием `Dash` или `Streamlit`).
- [ ] Добавить логирование всех сделок (в CSV или SQLite базу данных) для последующего анализа PnL (Прибыль/Убыток).
- [ ] Настроить уведомления в Telegram (через бота) об открытых сделках и критических ошибках.